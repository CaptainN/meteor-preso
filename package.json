{
  "name": "meteor-preso",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "debug": "meteor run --inspect",
    "debug-brk": "meteor run --inspect-brk"
  },
  "dependencies": {
    "babel-runtime": "^6.26.0",
    "bcrypt": "^2.0.1",
    "meteor-node-stubs": "^0.3.3"
  }
}
